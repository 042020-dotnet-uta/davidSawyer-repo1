@model MvcMovie.Models.Order

@{
    ViewData["Title"] = "NewOrder";
}

<h1>New Order</h1>

<h4>Order</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="NewOrder">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="OrderDate" class="control-label">Order Time</label>
                <span asp-validation-for="OrderDate" class="text-danger" id="time"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Order Time</label>
                <span asp-validation-for="OrderDate" class="text-danger" id="time"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
